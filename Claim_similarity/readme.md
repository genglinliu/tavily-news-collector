we categorize claims by their semantic similarity to questions.

we use the sentence transformer to encode the claims and questions.

we then calculate the cosine similarity between the claim and question embeddings.